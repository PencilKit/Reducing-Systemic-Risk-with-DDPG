# a <- c(runif(n_b,ab_l,smal_scale*ab_u), runif(n_m,am_l,smal_scale*am_u), runif(n_s,as_l,smal_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
print(a)
print(l)
n <- length(a)
a[n+1] <- if(sum(l)-sum(a) >0){sum(l)-sum(a)}else{0}
l[n+1] <- if(sum(a)-sum(l) >0){sum(a)-sum(l)}else{0}
n <- length(a)
E <- (a+l)/n # This should be n?
# use the systemicrisk package
mod <- Model.additivelink.exponential.fitness(n,alpha=-2.5,beta=0.4,gamma=0.8,
lambdaprior=Model.fitness.genlambdaparprior(ratescale=20))
thin <-100
print(sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE))
res <- sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE)
# rounding the variables to two digits (monetary units)
count <- 0
for (L in res$L){
# print(L)
count <- count + 1
L_file_n = paste("R_liability_network", count, sep="_")
write.csv(L, file.path(path_out_L, paste(L_file_n, ".csv", sep="")))
}
# L <- round(res$L[[5]],2)
# a <- colSums(L)
# l <- rowSums(L)
# E <- round(E,2)
# kappa <- round(l/E,2)
E <- round(E,2)
E_file_n <- "R_networth_values"
write.csv(E, file.path(path_out_E, paste(E_file_n, ".csv", sep="")))
print("DONE")
library(systemicrisk)
rm(list=ls())
path_out_L <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_initial_networks"
path_out_E <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_networth"
# set.seed(50)
# n_b <- 2; n_m <- 3; n_s <- 25 # 30
# n_b <- 3; n_m <- 5; n_s <- 41 # 49/50
# n_b <- 1; n_m <- 2; n_s <- 7 # 10
# n_b <- 2; n_m <- 3; n_s <- 15 # 20
n_b <- 30 # 100 for the uniform
#########################################
# ab_l <- 6000
# ab_u <- 10000
#
# am_l <- 2000
# am_u <- 6000
#
# as_l <- 500
# as_u <- 2000
#
# lb_l <- 0
# lb_u <- 2000
#
# lm_l <- 0
# lm_u <- 700
#
# ls_l <- 0
# ls_u <- 150
#########################################
# For N=100 leverage experiment
ab_l <- 9000 # 9000
ab_u <- 10000
lb_l <- 1000
lb_u <- 2000
gu_scale <- 1.0 # For N=100
gl_scale <- 1.0 # For N=100
a <- runif(n_b,gl_scale*ab_l,gu_scale*ab_u)
l <- a + rnorm(n_b,lb_l,lb_u)
# GIGANTIC layer
# gu_scale <- 20.0
# gl_scale <- 20.0
# Magnitude is for N=20's
# gu_scale <- 10.0 # magnitude 0.01, use 20/20 for N=10
# gl_scale <- 10.0 # 2.0/1.0/0.5 # 10 was for N=20 for different
# Magnitude is for N=10's
#
# print(giga_scale)
# a <- c(runif(n_b,gl_scale*ab_l,gu_scale*ab_u), runif(n_m,gl_scale*am_l,gu_scale*am_u), runif(n_s,gl_scale*as_l,gu_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MAx layer
# a <- c(runif(n_b, ab_l, ab_u), runif(n_m, am_l, am_u), runif(n_s,as_l,as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MED layer
# m_scale <- 0.1 # magnitude
# mm_cale <- 2.9 # micro adjustments
# m_scale <- 1.0 # magnitude
# mm_cale <- 0.4 # micro adjustments
# med_scale <- m_scale* mm_scale
# a <- c(runif(n_b, ab_l, med_scale*ab_u), runif(n_m,am_l, med_scale*am_u), runif(n_s,as_l, med_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# SM layer
# s_scale <- 0.1 # magnitude 0.01
# sm_scale <- 1.5
# smal_scale <- s_scale * sm_scale
# a <- c(runif(n_b,ab_l,smal_scale*ab_u), runif(n_m,am_l,smal_scale*am_u), runif(n_s,as_l,smal_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
print(a)
print(l)
n <- length(a)
a[n+1] <- if(sum(l)-sum(a) >0){sum(l)-sum(a)}else{0}
l[n+1] <- if(sum(a)-sum(l) >0){sum(a)-sum(l)}else{0}
n <- length(a)
E <- (a+l)/n # This should be n?
# use the systemicrisk package
mod <- Model.additivelink.exponential.fitness(n,alpha=-2.5,beta=0.4,gamma=0.8,
lambdaprior=Model.fitness.genlambdaparprior(ratescale=20))
thin <-100
print(sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE))
res <- sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE)
# rounding the variables to two digits (monetary units)
count <- 0
for (L in res$L){
# print(L)
count <- count + 1
L_file_n = paste("R_liability_network", count, sep="_")
write.csv(L, file.path(path_out_L, paste(L_file_n, ".csv", sep="")))
}
# L <- round(res$L[[5]],2)
# a <- colSums(L)
# l <- rowSums(L)
# E <- round(E,2)
# kappa <- round(l/E,2)
E <- round(E,2)
E_file_n <- "R_networth_values"
write.csv(E, file.path(path_out_E, paste(E_file_n, ".csv", sep="")))
print("DONE")
library(systemicrisk)
rm(list=ls())
path_out_L <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_initial_networks"
path_out_E <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_networth"
# set.seed(50)
# n_b <- 2; n_m <- 3; n_s <- 25 # 30
# n_b <- 3; n_m <- 5; n_s <- 41 # 49/50
# n_b <- 1; n_m <- 2; n_s <- 7 # 10
# n_b <- 2; n_m <- 3; n_s <- 15 # 20
n_b <- 20 # 100 for the uniform
#########################################
# ab_l <- 6000
# ab_u <- 10000
#
# am_l <- 2000
# am_u <- 6000
#
# as_l <- 500
# as_u <- 2000
#
# lb_l <- 0
# lb_u <- 2000
#
# lm_l <- 0
# lm_u <- 700
#
# ls_l <- 0
# ls_u <- 150
#########################################
# For N=100 leverage experiment
ab_l <- 9000 # 9000
ab_u <- 10000
lb_l <- 1000
lb_u <- 2000
gu_scale <- 1.0 # For N=100
gl_scale <- 1.0 # For N=100
a <- runif(n_b,gl_scale*ab_l,gu_scale*ab_u)
l <- a + rnorm(n_b,lb_l,lb_u)
# GIGANTIC layer
# gu_scale <- 20.0
# gl_scale <- 20.0
# Magnitude is for N=20's
# gu_scale <- 10.0 # magnitude 0.01, use 20/20 for N=10
# gl_scale <- 10.0 # 2.0/1.0/0.5 # 10 was for N=20 for different
# Magnitude is for N=10's
#
# print(giga_scale)
# a <- c(runif(n_b,gl_scale*ab_l,gu_scale*ab_u), runif(n_m,gl_scale*am_l,gu_scale*am_u), runif(n_s,gl_scale*as_l,gu_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MAx layer
# a <- c(runif(n_b, ab_l, ab_u), runif(n_m, am_l, am_u), runif(n_s,as_l,as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MED layer
# m_scale <- 0.1 # magnitude
# mm_cale <- 2.9 # micro adjustments
# m_scale <- 1.0 # magnitude
# mm_cale <- 0.4 # micro adjustments
# med_scale <- m_scale* mm_scale
# a <- c(runif(n_b, ab_l, med_scale*ab_u), runif(n_m,am_l, med_scale*am_u), runif(n_s,as_l, med_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# SM layer
# s_scale <- 0.1 # magnitude 0.01
# sm_scale <- 1.5
# smal_scale <- s_scale * sm_scale
# a <- c(runif(n_b,ab_l,smal_scale*ab_u), runif(n_m,am_l,smal_scale*am_u), runif(n_s,as_l,smal_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
print(a)
print(l)
n <- length(a)
a[n+1] <- if(sum(l)-sum(a) >0){sum(l)-sum(a)}else{0}
l[n+1] <- if(sum(a)-sum(l) >0){sum(a)-sum(l)}else{0}
n <- length(a)
E <- (a+l)/n # This should be n?
# use the systemicrisk package
mod <- Model.additivelink.exponential.fitness(n,alpha=-2.5,beta=0.4,gamma=0.8,
lambdaprior=Model.fitness.genlambdaparprior(ratescale=20))
thin <-100
print(sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE))
res <- sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE)
# rounding the variables to two digits (monetary units)
count <- 0
for (L in res$L){
# print(L)
count <- count + 1
L_file_n = paste("R_liability_network", count, sep="_")
write.csv(L, file.path(path_out_L, paste(L_file_n, ".csv", sep="")))
}
# L <- round(res$L[[5]],2)
# a <- colSums(L)
# l <- rowSums(L)
# E <- round(E,2)
# kappa <- round(l/E,2)
E <- round(E,2)
E_file_n <- "R_networth_values"
write.csv(E, file.path(path_out_E, paste(E_file_n, ".csv", sep="")))
print("DONE")
library(systemicrisk)
rm(list=ls())
path_out_L <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_initial_networks"
path_out_E <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_networth"
# set.seed(50)
# n_b <- 2; n_m <- 3; n_s <- 25 # 30
# n_b <- 3; n_m <- 5; n_s <- 41 # 49/50
# n_b <- 1; n_m <- 2; n_s <- 7 # 10
# n_b <- 2; n_m <- 3; n_s <- 15 # 20
n_b <- 20 # 100 for the uniform
#########################################
# ab_l <- 6000
# ab_u <- 10000
#
# am_l <- 2000
# am_u <- 6000
#
# as_l <- 500
# as_u <- 2000
#
# lb_l <- 0
# lb_u <- 2000
#
# lm_l <- 0
# lm_u <- 700
#
# ls_l <- 0
# ls_u <- 150
#########################################
# For N=100 leverage experiment
ab_l <- 9000 # 9000
ab_u <- 10000
lb_l <- 1000
lb_u <- 2000
gu_scale <- 1.0 # For N=100
gl_scale <- 1.0 # For N=100
a <- runif(n_b,gl_scale*ab_l,gu_scale*ab_u)
l <- a + rnorm(n_b,lb_l,lb_u)
# GIGANTIC layer
# gu_scale <- 20.0
# gl_scale <- 20.0
# Magnitude is for N=20's
# gu_scale <- 10.0 # magnitude 0.01, use 20/20 for N=10
# gl_scale <- 10.0 # 2.0/1.0/0.5 # 10 was for N=20 for different
# Magnitude is for N=10's
#
# print(giga_scale)
# a <- c(runif(n_b,gl_scale*ab_l,gu_scale*ab_u), runif(n_m,gl_scale*am_l,gu_scale*am_u), runif(n_s,gl_scale*as_l,gu_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MAx layer
# a <- c(runif(n_b, ab_l, ab_u), runif(n_m, am_l, am_u), runif(n_s,as_l,as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MED layer
# m_scale <- 0.1 # magnitude
# mm_cale <- 2.9 # micro adjustments
# m_scale <- 1.0 # magnitude
# mm_cale <- 0.4 # micro adjustments
# med_scale <- m_scale* mm_scale
# a <- c(runif(n_b, ab_l, med_scale*ab_u), runif(n_m,am_l, med_scale*am_u), runif(n_s,as_l, med_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# SM layer
# s_scale <- 0.1 # magnitude 0.01
# sm_scale <- 1.5
# smal_scale <- s_scale * sm_scale
# a <- c(runif(n_b,ab_l,smal_scale*ab_u), runif(n_m,am_l,smal_scale*am_u), runif(n_s,as_l,smal_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
print(a)
print(l)
n <- length(a)
a[n+1] <- if(sum(l)-sum(a) >0){sum(l)-sum(a)}else{0}
l[n+1] <- if(sum(a)-sum(l) >0){sum(a)-sum(l)}else{0}
n <- length(a)
E <- (a+l)/n # This should be n?
# use the systemicrisk package
mod <- Model.additivelink.exponential.fitness(n,alpha=-2.5,beta=0.4,gamma=0.8,
lambdaprior=Model.fitness.genlambdaparprior(ratescale=20))
thin <-100
print(sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE))
res <- sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE)
# rounding the variables to two digits (monetary units)
count <- 0
for (L in res$L){
# print(L)
count <- count + 1
L_file_n = paste("R_liability_network", count, sep="_")
write.csv(L, file.path(path_out_L, paste(L_file_n, ".csv", sep="")))
}
# L <- round(res$L[[5]],2)
# a <- colSums(L)
# l <- rowSums(L)
# E <- round(E,2)
# kappa <- round(l/E,2)
E <- round(E,2)
E_file_n <- "R_networth_values"
write.csv(E, file.path(path_out_E, paste(E_file_n, ".csv", sep="")))
print("DONE")
library(systemicrisk)
rm(list=ls())
path_out_L <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_initial_networks"
path_out_E <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_networth"
# set.seed(50)
# n_b <- 2; n_m <- 3; n_s <- 25 # 30
# n_b <- 3; n_m <- 5; n_s <- 41 # 49/50
# n_b <- 1; n_m <- 2; n_s <- 7 # 10
# n_b <- 2; n_m <- 3; n_s <- 15 # 20
n_b <- 10 # 100 for the uniform
#########################################
# ab_l <- 6000
# ab_u <- 10000
#
# am_l <- 2000
# am_u <- 6000
#
# as_l <- 500
# as_u <- 2000
#
# lb_l <- 0
# lb_u <- 2000
#
# lm_l <- 0
# lm_u <- 700
#
# ls_l <- 0
# ls_u <- 150
#########################################
# For N=100 leverage experiment
ab_l <- 9000 # 9000
ab_u <- 10000
lb_l <- 1000
lb_u <- 2000
gu_scale <- 1.0 # For N=100
gl_scale <- 1.0 # For N=100
a <- runif(n_b,gl_scale*ab_l,gu_scale*ab_u)
l <- a + rnorm(n_b,lb_l,lb_u)
# GIGANTIC layer
# gu_scale <- 20.0
# gl_scale <- 20.0
# Magnitude is for N=20's
# gu_scale <- 10.0 # magnitude 0.01, use 20/20 for N=10
# gl_scale <- 10.0 # 2.0/1.0/0.5 # 10 was for N=20 for different
# Magnitude is for N=10's
#
# print(giga_scale)
# a <- c(runif(n_b,gl_scale*ab_l,gu_scale*ab_u), runif(n_m,gl_scale*am_l,gu_scale*am_u), runif(n_s,gl_scale*as_l,gu_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MAx layer
# a <- c(runif(n_b, ab_l, ab_u), runif(n_m, am_l, am_u), runif(n_s,as_l,as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MED layer
# m_scale <- 0.1 # magnitude
# mm_cale <- 2.9 # micro adjustments
# m_scale <- 1.0 # magnitude
# mm_cale <- 0.4 # micro adjustments
# med_scale <- m_scale* mm_scale
# a <- c(runif(n_b, ab_l, med_scale*ab_u), runif(n_m,am_l, med_scale*am_u), runif(n_s,as_l, med_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# SM layer
# s_scale <- 0.1 # magnitude 0.01
# sm_scale <- 1.5
# smal_scale <- s_scale * sm_scale
# a <- c(runif(n_b,ab_l,smal_scale*ab_u), runif(n_m,am_l,smal_scale*am_u), runif(n_s,as_l,smal_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
print(a)
print(l)
n <- length(a)
a[n+1] <- if(sum(l)-sum(a) >0){sum(l)-sum(a)}else{0}
l[n+1] <- if(sum(a)-sum(l) >0){sum(a)-sum(l)}else{0}
n <- length(a)
E <- (a+l)/n # This should be n?
# use the systemicrisk package
mod <- Model.additivelink.exponential.fitness(n,alpha=-2.5,beta=0.4,gamma=0.8,
lambdaprior=Model.fitness.genlambdaparprior(ratescale=20))
thin <-100
print(sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE))
res <- sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE)
# rounding the variables to two digits (monetary units)
count <- 0
for (L in res$L){
# print(L)
count <- count + 1
L_file_n = paste("R_liability_network", count, sep="_")
write.csv(L, file.path(path_out_L, paste(L_file_n, ".csv", sep="")))
}
# L <- round(res$L[[5]],2)
# a <- colSums(L)
# l <- rowSums(L)
# E <- round(E,2)
# kappa <- round(l/E,2)
E <- round(E,2)
E_file_n <- "R_networth_values"
write.csv(E, file.path(path_out_E, paste(E_file_n, ".csv", sep="")))
print("DONE")
library(systemicrisk)
rm(list=ls())
path_out_L <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_initial_networks"
path_out_E <- "C:/Users/richa/My Drive/York/PhD Research/Systemic Risk/data/R Folder/Systemic Risk MILP/R_networth"
# set.seed(50)
# n_b <- 2; n_m <- 3; n_s <- 25 # 30
# n_b <- 3; n_m <- 5; n_s <- 41 # 49/50
# n_b <- 1; n_m <- 2; n_s <- 7 # 10
# n_b <- 2; n_m <- 3; n_s <- 15 # 20
n_b <- 10 # 100 for the uniform
#########################################
# ab_l <- 6000
# ab_u <- 10000
#
# am_l <- 2000
# am_u <- 6000
#
# as_l <- 500
# as_u <- 2000
#
# lb_l <- 0
# lb_u <- 2000
#
# lm_l <- 0
# lm_u <- 700
#
# ls_l <- 0
# ls_u <- 150
#########################################
# For N=100 leverage experiment
ab_l <- 9000 # 9000
ab_u <- 10000
lb_l <- 1000
lb_u <- 2000
gu_scale <- 1.0 # For N=100
gl_scale <- 1.0 # For N=100
a <- runif(n_b,gl_scale*ab_l,gu_scale*ab_u)
l <- a + rnorm(n_b,lb_l,lb_u)
# GIGANTIC layer
# gu_scale <- 20.0
# gl_scale <- 20.0
# Magnitude is for N=20's
# gu_scale <- 10.0 # magnitude 0.01, use 20/20 for N=10
# gl_scale <- 10.0 # 2.0/1.0/0.5 # 10 was for N=20 for different
# Magnitude is for N=10's
#
# print(giga_scale)
# a <- c(runif(n_b,gl_scale*ab_l,gu_scale*ab_u), runif(n_m,gl_scale*am_l,gu_scale*am_u), runif(n_s,gl_scale*as_l,gu_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MAx layer
# a <- c(runif(n_b, ab_l, ab_u), runif(n_m, am_l, am_u), runif(n_s,as_l,as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# # MED layer
# m_scale <- 0.1 # magnitude
# mm_cale <- 2.9 # micro adjustments
# m_scale <- 1.0 # magnitude
# mm_cale <- 0.4 # micro adjustments
# med_scale <- m_scale* mm_scale
# a <- c(runif(n_b, ab_l, med_scale*ab_u), runif(n_m,am_l, med_scale*am_u), runif(n_s,as_l, med_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
# SM layer
# s_scale <- 0.1 # magnitude 0.01
# sm_scale <- 1.5
# smal_scale <- s_scale * sm_scale
# a <- c(runif(n_b,ab_l,smal_scale*ab_u), runif(n_m,am_l,smal_scale*am_u), runif(n_s,as_l,smal_scale*as_u))
# l <- a + c(rnorm(n_b,lb_l,lb_u),rnorm(n_m,lm_l,lm_u),rnorm(n_s,ls_l,ls_u))
print(a)
print(l)
n <- length(a)
a[n+1] <- if(sum(l)-sum(a) >0){sum(l)-sum(a)}else{0}
l[n+1] <- if(sum(a)-sum(l) >0){sum(a)-sum(l)}else{0}
n <- length(a)
E <- (a+l)/n # This should be n?
# use the systemicrisk package
mod <- Model.additivelink.exponential.fitness(n,alpha=-2.5,beta=0.4,gamma=0.8,
lambdaprior=Model.fitness.genlambdaparprior(ratescale=20))
thin <-100
print(sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE))
res <- sample_HierarchicalModel(l=l, a=a, model=mod, nsamples=10000, thin=thin, silent=TRUE)
# rounding the variables to two digits (monetary units)
count <- 0
for (L in res$L){
# print(L)
count <- count + 1
L_file_n = paste("R_liability_network", count, sep="_")
write.csv(L, file.path(path_out_L, paste(L_file_n, ".csv", sep="")))
}
# L <- round(res$L[[5]],2)
# a <- colSums(L)
# l <- rowSums(L)
# E <- round(E,2)
# kappa <- round(l/E,2)
E <- round(E,2)
E_file_n <- "R_networth_values"
write.csv(E, file.path(path_out_E, paste(E_file_n, ".csv", sep="")))
print("DONE")
